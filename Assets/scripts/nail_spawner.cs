using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class nail_spawner : MonoBehaviour
{
    //public Transform[] spawn_points;
    //public GameObject nails_prefab;

    //public float time_to_spawn = 2f;
    //float timer;

    //private void Start()
    //{
    //    timer = time_to_spawn;
    //}
    //private void Update()
    //{
    //    timer -= Time.deltaTime;
    //    if (timer <= 0)
    //    {
    //        spawn_them();
    //        timer = time_to_spawn;
    //    }
        
    //}

    //void spawn_them()
    //{
    //    int random_index = Random.Range(0, spawn_points.Length);
    //    for (int i = 0; i < spawn_points.Length; i++)
    //    {
    //        if(random_index != i)
    //        {
    //            Instantiate(nails_prefab, spawn_points[i].position, Quaternion.identity);
    //        }
    //    }
    //}
}
